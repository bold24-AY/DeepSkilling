# Exercise 1: Mocking and Stubbing

## Scenario
Test a service that depends on an external API using Mockito to mock and stub.

## Steps
1. Create a mock object for `ExternalApi`.
2. Stub its `getData()` method to return `"Mock Data"`.
3. Verify that `MyService` uses the mock object correctly.
