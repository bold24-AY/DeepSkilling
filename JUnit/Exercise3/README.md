# Exercise 3: Assertions in JUnit

## Scenario
Use different assertions in JUnit to validate test results.

## Steps
1. Create a class `AssertionsTest`.
2. Write a test case `testAssertions` to test various assertions: `assertEquals`, `assertTrue`, `assertFalse`, `assertNull`, and `assertNotNull`.
